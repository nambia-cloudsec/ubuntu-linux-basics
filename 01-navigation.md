# Linux Foundations - Phase 1: Navigation & Basics

## Core Concepts
- Linux is multi-user, permission-based, case-sensitive
- `/` is root, `~` is home, `.` current directory, `..` parent directory

## Commands Learned
pwd
ls
ls -l
ls -la
cd
cd ..
cd ~
cd /

mkdir foldername
mkdir -p parent/child
touch filename.txt
cp file1 file2
cp -r folder1 folder2
mv oldname newname
rm file.txt
rm -r foldername

cat file.txt
less file.txt
head file.txt
tail file.txt
tail -f file.txt

## Practice Exercises Completed
- Created directory structure
- Navigated between directories
- Renamed and deleted files

## Reflection
Understanding directory navigation is critical before learning permissions and system internals.
