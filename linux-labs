# Phase 3: Permissions & Ownership Exercises

# Exercise 1: Create files with different permissions
touch file1 file2
chmod 644 file1
chmod 600 file2

# Exercise 2: Change ownership (use sudo if necessary)
sudo chown root file1
sudo chown nambia-mangare file2

# Exercise 3: Explore directories
ls -l /etc
ls -ld /var /usr /home
